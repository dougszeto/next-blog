export enum Collections {
  ADMINS = "admins",
  USERNAMES = "usernames",
  USERS = "users",
  POSTS = "posts",
  HEARTS = "hearts",
}

export enum AdminTabs {
  CREATE = "create",
  VIEW = "view",
}
